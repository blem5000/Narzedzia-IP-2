<Window x:Class="NarzedziaIP.DhcpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rozwiązywanie hostname w DHCP"
        Width="520"
        Height="220"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI"
        FontSize="14"
        Loaded="Window_Loaded">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Adres serwera DHCP:"
                   Grid.Row="0"
                   Grid.Column="0"
                   FontWeight="Bold"
                   VerticalAlignment="Center"
                   Margin="0,0,10,0"/>

        <TextBox x:Name="txtServerIP"
                 Grid.Row="0"
                 Grid.Column="1"
                 Height="28"
                 VerticalContentAlignment="Center"/>

        <StackPanel Grid.Row="1"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    Margin="0,15,0,0">

            <ProgressBar x:Name="progressCheck"
                         Width="180"
                         Height="18"
                         IsIndeterminate="True"
                         Visibility="Collapsed"
                         Margin="0,0,10,0"/>

            <TextBlock x:Name="txtStatus"
                       VerticalAlignment="Center"
                       Text=""/>
        </StackPanel>

        <StackPanel Grid.Row="2"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,25,0,0">

            <Button x:Name="btnOK"
                    Content="OK"
                    Width="90"
                    Height="32"
                    Margin="0,0,10,0"
                    IsDefault="True"
                    Click="btnOK_Click"/>

            <Button x:Name="btnCancel"
                    Content="Anuluj"
                    Width="90"
                    Height="32"
                    IsCancel="True"
                    Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>